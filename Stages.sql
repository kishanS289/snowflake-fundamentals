-- USER STAGE
LIST @~;

-- TABLE STAGE
CREATE TABLE STAGE_TABLE(
    ID INT,
    NAME VARCHAR
);

LIST @%STAGE_TABLE;

SHOW STAGES;

-- INTERNAL STAGE
CREATE OR REPLACE STAGE STA_CSV
FILE_FORMAT=CSVTYPE;

LIST @STA_CSV;

DESC STAGE STA_CSV;

REMOVE @STA_CSV;

SELECT $1,$2,$3 FROM @STA_CSV;

SELECT $1,$2,$3 FROM @STA_CSV/Customers.csv;

CREATE OR REPLACE FILE FORMAT CSVTYPE
TYPE=CSV
SKIP_HEADER=1;













